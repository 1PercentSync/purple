using UnityEngine;

public class BombCameraLook : MonoBehaviour
{
    void Start()
    {
        transform.rotation = transform.rotation;
    }

    void Update()
    {
        // this isnt gonna be used just to lock the user in
    }
}
